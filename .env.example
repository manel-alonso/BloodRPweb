# Discord OAuth2 - Create an app at https://discord.com/developers/applications
VITE_DISCORD_CLIENT_ID=your_discord_application_client_id
VITE_DISCORD_REDIRECT_URI=http://localhost:5173/auth/callback

# API URL - Required for local dev (npm run dev). Point to your Vercel deployment URL.
# When deployed on Vercel, leave unset (same-origin). For GitHub Pages, set at build time.
# VITE_API_URL=https://your-app.vercel.app

# GitHub Pages - Base URL (ej: /BloodRPweb/ para proyecto, / para user page)
# VITE_BASE_URL=/BloodRPweb/

# Discord invite - Enlace para unirse al servidor (usado en Whitelist)
# VITE_DISCORD_INVITE_URL=https://discord.gg/tu-invite

# Whitelist API (Vercel server-side only) - Discord bot para verificar roles:
# DISCORD_BOT_TOKEN=tu_bot_token
# DISCORD_GUILD_ID=id_del_servidor_discord
# WHITELIST_ROLE_IDS=id_rol1,id_rol2  (roles que indican whitelist, separados por coma)
# WHITELIST_REVIEW_CHANNEL_ID=id_canal  (canal #whitelist-review para recibir solicitudes)
# WHITELIST_REVISION_ROLE_ID=id_rol  (rol WhitelistRevision para marcar solicitud enviada)
#
# IMPORTANTE - Jerarquía de roles: El rol del bot debe estar ARRIBA del rol WhitelistRevision
# en Configuración del servidor > Roles. Si el bot no puede asignar el rol, arrastra el rol
# del bot (o el rol Staff que tenga) por encima de WhitelistRevision en la lista.
